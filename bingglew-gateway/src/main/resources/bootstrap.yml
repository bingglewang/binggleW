server:
  port: 9999

spring:
  application:
    name: bingglew-gateway
  cloud:
    config:
      fail-fast: true
      name: ${spring.application.name}
      profile: ${spring.profiles.active}
      discovery:
        enabled: true
        service-id: bingglew-config
  profiles:
    active: dev
eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://bingglew:bingglew@localhost:8761/eureka/

